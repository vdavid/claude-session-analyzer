// Everything on this page comes from a Go server on this machine, so there's nothing to render
// ahead of time and nothing to index. `adapter-static` emits the one fallback page and the router
// takes it from there.
export const ssr = false
export const prerender = false
