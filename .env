# Ports for local development. Nothing here is secret, so the file is committed: a fresh clone runs and works.
# Both services bind to 127.0.0.1, never 0.0.0.0.
CSA_BACKEND_PORT=19427
CSA_FRONTEND_PORT=19428
