# Go
/claude-session-analyzer
*.test
*.out

# Node and the frontend build
node_modules/
.svelte-kit/
build/
dist/

# Local overrides. The committed `.env` holds the dev ports, which aren't secret.
.env.local
.env.*.local

# Worktrees live here, one per branch, and they're never part of the repo.
.claude/worktrees/

# Editors and OS
.idea/
.vscode/
.DS_Store
